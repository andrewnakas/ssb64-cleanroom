"""Hand-made art for textures where a colour grid is not enough (text, faces, portraits, icons).

hook(t) -> (h, w, 4) float RGBA or None (None = default grid + alpha texture).
"""


def hook(t):
    return None
